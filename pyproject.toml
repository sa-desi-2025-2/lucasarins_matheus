[project]
name = "roimaintenence"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "Matheus Jose", email = "viliczinskimatheusjose@gmail.com"},
]
dependencies = ["django<4.2", "pymysql>=1.1.2"]
requires-python = ">=3.13"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[tool.pdm]
distribution = true
